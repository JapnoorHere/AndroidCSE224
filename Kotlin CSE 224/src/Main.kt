fun main(){
    var a = 10;
    var b = 20;
    println("Sum of $a and $b is ${a + b}");
    println("Sum of " + a+ " and " + b + " is " + (a+b));


    var `if` = 30;
    print("Value of if is $`if`")
}

//String interpolation allows you to insert variable values directly in to a string using $ symbol